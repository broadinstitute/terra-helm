apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ index .Values "actions-runner-controller" "nameOverride" }}-psp-role
  labels:
    {{- include "dsp-gha-runner.labels" . | nindent 4 }}
rules:
- apiGroups: ["extensions"]
  resources: ["podsecuritypolicies"]
  verbs: ["use"]
  resourceNames:
  - {{ index .Values "actions-runner-controller" "nameOverride" }}-psp
