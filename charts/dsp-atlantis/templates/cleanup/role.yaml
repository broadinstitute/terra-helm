{{- if .Values.cleanup.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: atlantis-cleanup-role
  labels: 
    {{- include "dsp-atlantis.labels" . | nindent 4 }}
rules: 
- apiGroups: ["policy"]
  resources: ["podsecuritypolicies"]
  verbs: ["use"]
  resourceNames: ["atlantis-psp"]
{{- end }}
