{{- if .Values.cleanup.enabled }}
apiVersion: secrets-manager.tuenti.io/v1alpha1
kind: SecretDefinition
metadata:
  name: atlantis-secretdef
  labels: 
    {{- include "dsp-atlantis.labels" . | nindent 4 }}
spec:
  name: atlantis-cleanup-secrets
  keysMap:
    cleanup-credentials:
      key: {{ required "A valid vault.cleanup.key is required" .Values.vault.cleanup.key }}
      path: {{ required "A valid vault.cleanup.path is required" .Values.vault.cleanup.path }}
      encoding: base64
{{- end }}
