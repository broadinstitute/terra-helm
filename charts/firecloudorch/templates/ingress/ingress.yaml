{{- include "libchart.ingress" (list . "firecloudorch.ingress" )  -}}
{{- define "firecloudorch.ingress" -}}
spec:
{{- if empty .Values.ingress.cert.preSharedCerts }}
  tls:
  - secretName: {{ .Values.name }}-cert
{{- end }}
{{- end }}
