apiVersion: v1
kind: Service
metadata:
  labels:
    app: opendj
    {{ if not .Values.devDeploy }}{{ template "opendj.labels" }}{{ end }}
  name: opendj
spec:
  clusterIP: None
  ports:
  - port: 1389
    name: ldap
  - port: 1636
    name: ldaps
  - port: 4444
    name: admin
  selector:
    app: opendj
