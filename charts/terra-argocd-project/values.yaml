# -- Name of this project. Eg. "terra-dev", "terra-cluster-dev"
projectName: null

# -- Description for this project. Eg. "Applications for Terra dev environment"
description: null

# -- An array of destinations to which this project can deploy, in the form [{ namespace: "target-namespace", server: "https://master-api-address"}]
destinations: []

# -- Whether this environment or cluster requires a user to be suitable to make changes.
requireSuitable: false

# -- Optionally allow applications in this project to deploy cluster-wide resources, such as ClusterRoles
clusterResourceWhitelist: []
