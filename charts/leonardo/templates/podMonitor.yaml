apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: leonardo-jvm-monitor
  labels:
{{ include "leonardo.labels" . | indent 4 }}
spec:
  jobLabel: leonardo-jvm
  selector:
    matchLabels:
      app.kubernetes.io/component: leonardo
  podMetricsEndpoints:
    - port: metrics
