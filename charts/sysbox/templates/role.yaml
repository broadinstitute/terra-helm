apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: sysbox-psp-role
  labels:
    {{- include "sysbox.labels" . | nindent 4 }}
rules:
- apiGroups: ["extensions"]
  resources: ["podsecuritypolicies"]
  verbs: ["use"]
  resourceNames:
  - sysbox-psp
