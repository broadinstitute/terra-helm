apiVersion: "v1"
kind: "Service"
metadata:
  name: "kernel-service-poc-service"
  labels:
    {{- template "poc.labels" }}
spec:
  ports:
  - name: "http"
    port: 8080
  selector:
    app: "kernel-service-poc"
