{{- include "libchart.service" (list . "testrunnerdashboard.service" )}}

{{- define "testrunnerdashboard.service" -}}
spec:
  selector:
    deployment: {{ .Values.ingress.deployment }}-deployment
  ports:
  - name: https
    protocol: TCP
    port: 443
    targetPort: 8050
{{- end }}
