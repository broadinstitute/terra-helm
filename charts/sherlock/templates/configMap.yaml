apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.global.name }}-cm
  labels:
    {{- include "sherlock.labels" . | nindent 4 }}
data:
  sherlock.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
