{{- include "libchart.ingress" (list . "thurloe.ingress" )  -}}
{{- define "thurloe.ingress" -}}
spec:
{{- if empty .Values.ingress.cert.preSharedCerts }}
  tls:
  - secretName: {{ .Values.name }}-cert
{{- end }}
{{- end }}
