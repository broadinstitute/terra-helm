apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: {{ .Values.name }}-pdb
spec:
  # don't reduce serving capacity by more than 30%
  minAvailable: 30%
  selector:
    matchLabels:
      deployment: {{ .Values.name }}-deployment
