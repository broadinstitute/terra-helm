apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: agora-jvm-monitor
  labels:
{{ include "agora.labels" . | indent 4 }}
spec:
  jobLabel: agora-jvm
  selector:
    matchLabels:
      app.kubernetes.io/component: agora
  podMetricsEndpoints:
    - port: metrics
