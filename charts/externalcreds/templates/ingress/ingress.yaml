{{- include "libchart.ingress" (list . "externalcreds.ingress" )  -}}
{{- define "externalcreds.ingress" -}}
spec:
{{- if empty .Values.ingress.cert.preSharedCerts }}
  tls:
  - secretName: {{ .Values.name }}-cert
{{- end }}
{{- end }}
