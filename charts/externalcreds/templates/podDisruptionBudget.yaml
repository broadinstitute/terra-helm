{{- include "libchart.pdb" (list . "externalcreds.pdb" ) }}

{{- define "externalcreds.pdb" -}}
spec:
  selector:
    matchLabels:
      deployment: {{ $.Values.name }}-{{ .name }}-deployment
{{- end }}
