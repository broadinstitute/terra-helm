apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.name }}-allowed-jwks
  labels:
{{ include "externalcreds.labels" . | indent 4 }}
data:
  allowed-jwks.yaml: |
    externalcreds.allowed-jwks-uris: [{{ join "," .Values.allowedJwks }}]
