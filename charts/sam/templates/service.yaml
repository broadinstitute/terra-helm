apiVersion: v1
kind: Service
metadata:
  name: sam
  labels:
    {{ if not .Values.devDeploy }}{{ template "sam.labels" }}{{ end }}
  ports:
    - name: http
      port: 8080
  selector:
    app: sam-service
