apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.name }}-configmap
  labels:
{{ include "sam.labels" . | indent 4 }}
data:
{{- if .Values.prometheus.enabled }}
  prometheusJmx.yaml: |
{{ include "sam.config.prometheusJmx" . | indent 4 }}
{{- end }}
