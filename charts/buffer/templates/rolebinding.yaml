apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: terra-buffer-service-role-binding
  labels:
    {{ template "buffer.labels" . }}
roleRef:
  kind: Role
  name: terra-buffer-service-role
  apiGroup: rbac.authorization.k8s.io
subjects:
# Authorize specific service accounts:
- kind: ServiceAccount
  name: terra-buffer-service-sa
