{{- include "libchart.ingress" (list . "buffer.ingress" )  -}}
spec:
{{- if empty .Values.ingress.cert.preSharedCerts }}
  tls:
  - secretName: {{ .Values.name }}-cert
{{- end }}
{{- define "buffer.ingress" -}}
