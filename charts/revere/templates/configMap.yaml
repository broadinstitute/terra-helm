apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.global.name }}-cm
  labels:
    {{- include "revere.labels" . | nindent 4 }}
data:
  revere.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
