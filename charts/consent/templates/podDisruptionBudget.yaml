{{- include "libchart.pdb" (list . "consent.pdb" ) }}

{{ define "consent.pdd" -}}
spec:
  selector:
    matchLabels:
      app: {{ .Chart.Name }}
      app.kubernetes.io/component: {{ .Chart.Name }}
      deployment: {{ .Chart.Name }}-deployment
{{- end }}
