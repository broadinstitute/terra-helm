apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-sqlproxy-secrets
  labels:
{{ include "consent.labels" . | indent 4 }}
data:
  googleProject: {{ .Values.googleProject }}
  cloudsqlZone: {{ .Values.googleProjectZone  }}
  cloudsqlInstance: {{ .Values.cloudSqlInstance  }}
