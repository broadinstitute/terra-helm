{{- include "libchart.service" (list . "consent.service" )  -}}
{{- define "consent.service" -}}
metadata:
  labels:
    app: {{ .Chart.Name }}
spec:
  selector:
    app: {{ .Chart.Name }}
{{- end }}
