apiVersion: v1
kind: Service
metadata:
  name: consent-ontology
  labels:
  {{ if not .Values.devDeploy }}{{ template "ontology.labels" }}{{ end }}
spec:
  ports:
    - name: http
      port: 8080
  selector:
    app: ontology-service
