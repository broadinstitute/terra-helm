apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  labels:
{{ include "ontology.labels" . | indent 4 }}
spec:
  ports:
    - name: http
      port: 8080
  selector:
    app: {{ .Chart.Name }}-service
