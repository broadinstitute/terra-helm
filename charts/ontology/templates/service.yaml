apiVersion: v1
kind: Service
metadata:
  name: ontology
  labels:
  {{ if not .Values.devDeploy }}{{ template "ontology.labels" . | indent 2 }}{{ end }}
spec:
  ports:
    - name: http
      port: 8080
  selector:
    app: ontology-service
