{{- if .Values.vault.enabled }}
apiVersion: secrets-manager.tuenti.io/v1alpha1
kind: SecretDefinition
metadata:
  name: ontology-secrets-sentry
  labels:
{{ include "ontology.labels" . | indent 4 }}
spec:
  name: ontology-sentry-dsn
  keysMap:
    dsn:
      path: {{ .Values.sentry.dsn.path }}
      key: {{ .Values.sentry.dsn.key }}
---
apiVersion: secrets-manager.tuenti.io/v1alpha1
kind: SecretDefinition
metadata:
  name: ontology-secrets-gcs
  labels:
{{ include "ontology.labels" . | indent 4 }}
spec:
  name: ontology-gcs-account
  keysMap:
    service-account.json:
      path: {{ .Values.gcsAccount.path }}
      encoding: base64
      key: {{ .Values.gcsAccount.key }}
---
{{- end }}
