apiVersion: secrets-manager.tuenti.io/v1alpha1
kind: SecretDefinition
metadata:
  name: mongodb-secretdef
  labels:
{{ include "mongodb.labels" . | indent 4 }}
spec:
  name: {{ .Values.name }}-secrets
  keysMap:
    {{ .Values.name }}-password:
      key: {{ required "A valid vaultAgoraPasswordKey value is required" .Values.vaultAgoraPasswordKey }}
      path: {{ required "A valid vaultAgoraPasswordPath value is required" .Values.vaultAgoraPasswordPath }}
    {{ .Values.name }}-root-password:
      key: rootPassword
      path: {{ required "A valid vaultSecretsPath value is required" .Values.vaultSecretsPath }}
    {{ .Values.name }}-replica-set-key:
      key: replicaSetKey
      path: {{ required "A valid vaultSecretsPath value is required" .Values.vaultSecretsPath }}
