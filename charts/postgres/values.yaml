# appVersion -- (string) Service image version/tag
appVersion: v1
name: sam

port: 5432

imageConfig:
  # imageConfig.repository -- Image repository
  repository: postgres
  # imageConfig.tag -- (string) Image tag
  tag: 9.6

env:
  # db username
  username: "sam"
  # db password
  password: "globochem"
  # db name
  db_name: "sam"
  # db data path
  data_path: /data/pgdata
